"""OS-integration helpers (open file, reveal in file manager).

Named ``platform`` per the project layout; uses ``sys.platform`` internally so
it never shadows the stdlib ``platform`` module in practice.
"""
